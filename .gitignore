# Ansible Vault — never commit unencrypted secrets
inventory/group_vars/all/vault.yml
.vault_password
.vault_pass

# SSH keys and certificates
*.pem
*.key
*.crt
id_rsa
id_ed25519

# Ansible retry files
*.retry

# Local overrides
*.local.yml
*.local.yaml

# Reference clones (local only — not part of the project)
reference/

# Per-run rendered file copies (env-specific values — audit locally, don't commit)
artifacts/

# Local AI-assistant context — not part of the enablement deliverable
CLAUDE.md

# Editor / OS
.DS_Store
*.swp
*~
.vscode/
